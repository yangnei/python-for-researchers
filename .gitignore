# Python
__pycache__/
*.pyc
.venv/
.venv-jlite/
venv/

# JupyterLite build working dir (the built app in docs/jupyter IS committed).
# Anchored to repo root so it does NOT match docs/jupyter/build/ (lab assets).
/build/
.jupyterlite.doit.db

# OS
.DS_Store
Thumbs.db

# Generated demo outputs (regenerated by running the demos)
examples/session-04/*_summary.csv
examples/session-04/snapshot.json

# PDF build intermediates (the .pdf files themselves are kept)
output/teacher-edition.html
docs/learn-python-student.html
